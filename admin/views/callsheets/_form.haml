.group
  =f.label :call_time
  =f.error_message_on :call_time
  =f.text_field :call_time, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :notes
  =f.error_message_on :notes
  =f.text_field :notes, :class => :text_field
  %span.description Ex: a simple text

=f.text_field_block :event_id
.group.navform.wat-cf
  =f.submit pat(:save), :class => :button
  =f.submit pat(:cancel), :onclick => "window.location='#{url(:callsheets, :index)}';return false", :class => :button
