.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:assigments, :index)
      %li.active=link_to pat(:new), url(:assigments, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:assigment)
    .inner
      -form_for :assigment, url(:assigments, :create), :class => :form do |f|
        =partial "assigments/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
