.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:streams, :index)
      %li.active=link_to pat(:new), url(:streams, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:stream)
    .inner
      -form_for :stream, url(:streams, :create), :class => :form do |f|
        =partial "streams/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
