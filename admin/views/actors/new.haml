.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:actors, :index)
      %li.active=link_to pat(:new), url(:actors, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:actor)
    .inner
      -form_for :actor, url(:actors, :create), :class => :form do |f|
        =partial "actors/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
