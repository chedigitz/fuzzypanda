.group
  =f.label :account_id
  =f.error_message_on :account_id
  =f.text_field :account_id, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :video_ids
  =f.error_message_on :video_ids
  =f.text_field :video_ids, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :event_ids
  =f.error_message_on :event_ids
  =f.text_field :event_ids, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :fbuid
  =f.error_message_on :fbuid
  =f.text_field :fbuid, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :scene_ids
  =f.error_message_on :scene_ids
  =f.text_field :scene_ids, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :fburl
  =f.error_message_on :fburl
  =f.text_field :fburl, :class => :text_field
  %span.description Ex: a simple text


.group.navform.wat-cf
  =f.submit pat(:save), :class => :button
  =f.submit pat(:cancel), :onclick => "window.location='#{url(:actors, :index)}';return false", :class => :button
